import XCTest
@testable import ApiHandlingModule

final class ApiHandlingModuleTests: XCTestCase {
    func popularMoviesTest() throws {
        let fetched = DataFetcher()
        //XCTAssertEqual(ApiHandlingModule().text, "Hello, World!")
    }
}
